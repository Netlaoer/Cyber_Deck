# FuyutsuiTools

**Fuyutsui 的功能扩展与数据覆盖模块**

FuyutsuiTools 是 [Fuyutsui](https://github.com/waynebian01/Fuyutsui/) 的非侵入式扩展插件，在不修改父插件源码的前提下，通过覆盖机制添加新功能、定制职业行为。

> **必读**：[README.md](README.md) — 完整架构、所有函数、数据结构、编码约定、扩展指南

## 目录结构

```
FuyutsuiTools/
├── FuyutsuiTools.toc        # 插件定义（依赖 Fuyutsui）
├── init.lua                 # 初始化钩子
├── main.lua                 # 血量实时轮询
├── logic_gui_Tools.py       # 主 GUI 入口（启动器，委托 Fuyutsui/ 子目录）
│
├── Fuyutsui/                # Python 主工具（原主插件 Fuyutsui/ 内的"请将我放到其他文件夹并重命名"目录）
│   ├── logic_gui.py         # 备用 GUI 入口
│   ├── utils.py             # 核心工具库
│   ├── GetPixels.py         # 屏幕像素扫描引擎
│   ├── config.yml           # 像素块配置（state/spells/groups 定义）
│   ├── class/               # 职业逻辑模块（13个职业）
│   ├── keymap/              # 按键映射（13个职业）
│   └── other/               # 调试工具 + icon.ico
│
├── laoer/                   # 覆盖模块（自动检测）
│   ├── overrides.py         # 覆盖加载引擎
│   ├── config.yml           # 覆盖配置
│   ├── class/               # 覆盖的职业逻辑
│   └── keymap/              # 覆盖的键位映射
│
├── core/                    # Lua 核心模块
│   ├── core.lua             # 驱散开关逻辑
│   ├── config.lua           # 配置覆盖（占位）
│   └── quickbutton.lua      # 四按钮面板
│
├── class/                   # Lua 职业数据块扩展
│   └── Paladin.lua          # 圣骑士数据块扩展
│
└── pack/                    # 打包工具
    ├── 打包exe.bat
    └── 打包exe.py
```

## 安装

1. 确保 [Fuyutsui](https://github.com/waynebian01/Fuyutsui/) 已正确安装
2. 将 `FuyutsuiTools` 文件夹复制到 `Interface/AddOns/` 目录
3. 确保目录结构为 `Interface/AddOns/FuyutsuiTools/FuyutsuiTools.toc`
4. 重载插件或重启游戏

## 运行

- **直接运行**：`python logic_gui_Tools.py` 或双击 `logic_gui_Tools.py`
- **打包成 exe**：运行 `pack/打包exe.bat`，需先安装 `pip install pyinstaller customtkinter PyYAML mss`
- `logic_gui_Tools.py` 会自动委托 `Fuyutsui/` 子目录中的所有逻辑，并自动检测 `laoer/` 覆盖模块

## 已实现功能

### 驱散开关
添加驱散开关，可通过游戏内按钮一键切换：
- **开启**：按原始逻辑自动驱散队伍成员
- **关闭**：神圣圣骑士仅对当前目标施放清洁术（魔法/疾病/中毒）

### 四按钮控制面板
替代父插件原有的单按钮，提供可拖动的四按钮面板：

| 按钮 | 功能 |
|------|------|
| 爆发 | 切换爆发开关 |
| 自动/单体 | 切换 AOE 模式 |
| 逻辑/辅助 | 切换输出模式 |
| 驱散 | 切换驱散开关 |

支持快捷键绑定（右键点击按钮进入绑定模式）。

### 圣骑士逻辑定制
- 驱散开关关闭时，神圣专精仅对目标类型为魔法(12)、疾病(13)、中毒(15)的当前目标驱散
- 制裁之锤直接映射为固定按键

### 血量实时轮询
- 覆盖 OnUpdate，以 0.2 秒间隔轮询玩家/目标/队伍血量
- 团本时自动回退到事件驱动，节省性能

## 覆盖机制

`logic_gui_Tools.py` 启动时自动扫描同级目录寻找含 `overrides.py` 的覆盖模块（如 `laoer/`），无需手动配置。

覆盖模块通过 monkey-patch 实现：
- **配置合并**：`config.yml` 深度合并到主配置
- **逻辑覆盖**：`class/` 中的同名 `.py` 文件优先于主工具加载
- **键位追加**：`keymap/` 中的条目追加到已加载的键位映射
