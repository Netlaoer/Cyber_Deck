# Cyber Deck（赛博义肢）

**Fuyutsui 的功能扩展与数据覆盖模块**

Cyber Deck 是 [Fuyutsui](https://github.com/waynebian01/Fuyutsui/) 的非侵入式扩展插件，在不修改父插件源码的前提下，通过覆盖机制添加新功能、定制职业行为。

> **必读**：[README.md](README.md) — 完整架构、所有函数、数据结构、编码约定、扩展指南

## 目录结构

```
Cyber_Deck/
├── Cyber_Deck.toc           # 插件定义（依赖 Fuyutsui）
├── init.lua                 # 初始化钩子（覆盖 OnEnable）
├── main.lua                 # 覆盖 main.lua 函数（5码敌人数、焦点引导修复等）
├── logic_gui_Tools.py       # 主 GUI 入口（启动器，委托 Fuyutsui/ 子目录）
│
├── Fuyutsui/                # Python 主工具（原主插件内的"请将我放到其他文件夹并重命名"目录，移动至此）
│   ├── config.yml           # 像素块配置（state/spells/groups 定义）
│   ├── utils.py             # 核心工具库（配置、按键、单位查询）
│   ├── GetPixels.py         # 屏幕像素扫描引擎
│   ├── class/               # 职业逻辑模块（13 个职业）
│   ├── keymap/              # 按键映射（13 个职业）
│   └── other/               # 调试工具
│
├── laoer/                   # 覆盖模块（自动检测，文件名可自定义）
│   ├── overrides.py         # 覆盖加载引擎（config 合并 + 模块覆盖 + keymap 追加）
│   ├── config.yml           # 覆盖配置（驱散开关、5码敌人）
│   ├── class/               # 覆盖的职业逻辑
│   ├── keymap/              # 覆盖的键位映射
│   └── other/               # icon.ico
│
├── core/                    # Lua 核心覆盖模块
│   ├── core.lua             # 驱散开关扩展 + db 注册
│   ├── config.lua           # 配置覆盖（占位）
│   └── quickbutton.lua      # 四按钮可拖拽面板
│
├── class/                   # Lua 职业数据块扩展
│   └── Paladin.lua          # 圣骑士像素块 + MacrosList 覆盖
│
└── pack/                    # 打包工具（开发者用）
```

## 安装

1. 确保 [Fuyutsui](https://github.com/waynebian01/Fuyutsui/) 已正确安装
2. 将 `Cyber_Deck` 文件夹复制到 `Interface/AddOns/` 目录
3. 确保目录结构为 `Interface/AddOns/Cyber_Deck/Cyber_Deck.toc`
4. **Lua 部分**：重载插件或重启游戏即生效
5. **Python 部分**：见下方"使用"章节

## 使用

`Cyber_Deck.exe` 可放在**任意位置**（桌面、C 盘等），双击运行即可。exe 会自动通过以下方式找到插件目录：

1. 读取注册表中的 WoW 安装路径
2. 查找正在运行的 WoW 进程位置
3. 自动加载 `Interface/AddOns/Cyber_Deck/Fuyutsui/` 中的配置和逻辑

同时自动检测同插件目录下的 `laoer/` 覆盖模块。

## 已实现功能

### 四按钮控制面板

替代父插件原有的单按钮，提供可拖拽的四按钮面板：

| 按钮 | 功能 | 说明 |
|------|------|------|
| 爆发 | `F:SwitchCooldown()` | 切换爆发技能开关 |
| 自动/单体 | `F:SwitchAoeMode()` | 切换 AOE / 单体模式 |
| 逻辑/辅助 | `F:SwitchDpsMode()` | 切换手动逻辑 / 官方一键辅助 |
| 驱散 | `F:SwitchDispel()` | 切换驱散开关 |

支持快捷键绑定（右键点击按钮进入绑定模式），位置持久化保存。

### 驱散开关

Lua + Python 双端覆盖实现：

- **开启**：按原始逻辑自动驱散队伍成员
- **关闭**：神圣圣骑士仅对当前目标施放清洁术（魔法/疾病/中毒）

### 圣骑士逻辑定制

- **驱散开关**：关闭时神圣专精仅对当前目标驱散（魔法/疾病/中毒），不自动驱散队友
- **制裁之锤**：直接映射为固定按键
- **焦点目标施法**（神圣专精）：审判和神圣震击优先对焦点目标施放，无焦点则打当前目标；正义盾击始终对玩家施放（不切目标）
- **目标类型绕过**：选中友方治疗时，进攻技能仍可正常施放（配合焦点目标宏）
- **5 豆正义盾击**：仅在 5 码内有姓名版敌人时施放，避免对空气浪费 GCD
- 扩展状态字段：`驱散开关`、`5码敌人`（通过 `laoer/config.yml` 配置）

## 覆盖机制

### Lua 端

| 文件 | 覆盖内容 |
|------|---------|
| `init.lua` | `F:OnEnable` — 进入世界时重新初始化 |
| `main.lua` | `F:updatePlayerConfig` / `F:updateEnemyCount` / `F:updateUnitCastingOrChannelingInfo` |
| `core/core.lua` | `F:SwitchDispel` — 驱散开关扩展 + db 注册 |
| `core/quickbutton.lua` | `F:InitQuickToggleButton` — 四按钮面板 |
| `class/Paladin.lua` | 神圣专精像素块扩展 + MacrosList 覆盖 |

### Python 端

`logic_gui_Tools.py` 启动时自动扫描同级目录寻找含 `overrides.py` 的覆盖模块（如 `laoer/`），无需手动配置。

覆盖模块通过 monkey-patch 实现：
- **配置合并**：`config.yml` 深度合并到主配置
- **逻辑覆盖**：`class/` 中的同名 `.py` 文件优先于主工具加载
- **键位追加**：`keymap/` 中的条目追加到已加载的键位映射

> 也可通过环境变量 `CYBER_DECK_OVERRIDE` 手动指定覆盖模块路径。

## 开发说明

- **不修改主插件源码**：所有功能通过覆盖机制实现
- **添加新职业逻辑**：在 `Fuyutsui/class/` 创建 `xxx_logic.py`，在 `Fuyutsui/keymap/` 创建 `xxx.yml`
- **覆盖已有逻辑**：在 `laoer/class/` 创建同名 `.py` 文件
- `Fuyutsui/` 目录下的文件来源于主插件更新，**不应直接编辑其中的**（下次更新会覆盖）
