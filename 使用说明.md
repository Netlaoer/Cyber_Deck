# Cyber Deck（赛博义肢）

**Fuyutsui 的功能扩展与数据覆盖模块**

Cyber Deck 是 [Fuyutsui](https://github.com/waynebian01/Fuyutsui/) 的非侵入式扩展插件，在不修改父插件源码的前提下，通过覆盖机制添加新功能、定制职业行为。

> **注意**：核心模块已加密打包，防止特征检测。定期更新 `Cyber_Deck.exe`。如需源码请联系作者。
>
> **开发者请参阅**：[README.md](README.md) — 完整架构、函数参考、数据结构、编码约定、扩展指南

---

## 安装

1. 确保 [Fuyutsui](https://github.com/waynebian01/Fuyutsui/) 已正确安装
2. 将 `Cyber_Deck` 文件夹复制到 `Interface/AddOns/` 目录
3. 重载插件或重启游戏，Lua 部分即生效
4. 双击 `Cyber_Deck.exe` 启动程序（已打包，无需安装 Python，可放任意位置）

---

## 使用

### 启动程序

- **移动到你想要的位置**：双击 `Cyber_Deck.exe` 直接启动（已打包，无需安装 Python，可放任意位置）
程序会自动找到 WoW 和插件目录，无需手动配置。

> 修改 Python 文件后点击 GUI 中的"重载"按钮即可生效，无需重启程序。

### GUI 界面

| 区域 | 说明 |
|------|------|
| 标题栏 | 赛博朋克风格，显示当前职业和专精 |
| 绑定按钮 | 默认 **鼠标侧键（XBUTTON1）**，按住时执行逻辑 |
| 重载按钮 | 修改 Python 文件后点击重载，无需重启程序 |
| 状态面板 | 实时显示游戏状态（血量、能量、冷却等） |
| 冷却面板 | 显示各技能冷却状态 |

### 按键发送模式

| 模式 | 说明 |
|------|------|
| `switch` | 模拟按键按下+释放（默认，适合瞬发技能） |
| `click` | 模拟鼠标点击 |
| `hold` | 按住不放（用于持续施法/引导技能） |

---

## 功能

### 四按钮控制面板

替代父插件原有的单按钮，提供可拖拽的四按钮面板：

| 按钮 | 功能 | 说明 |
|------|------|------|
| 爆发 | 切换爆发技能开关 | 控制是否使用长 CD 爆发技能 |
| 自动/单体 | 切换 AOE / 单体模式 | 自动判断或强制单体输出 |
| 逻辑/辅助 | 切换手动逻辑 / 官方一键辅助 | 切换输出模式 |
| 驱散 | 切换驱散开关 | 控制是否自动驱散队友 |

**操作提示**：
- 拖拽面板到任意位置，位置自动保存
- 右键点击按钮可重新绑定按键

### 驱散开关

- **开启**（默认）：自动驱散队伍中有 debuff 的成员
- **关闭**：神圣圣骑士仅对当前目标施放清洁术

### 圣骑士定制

- **焦点目标施法**：审判和神圣震击优先对焦点的目标施放(焦点可以设置为坦克)，无焦点则打当前目标
- **正义盾击**：始终对玩家自己施放（不切目标），仅在 5 码内有敌人时使用
- **制裁之锤**：直接映射为固定按键
- **选中友方时**：进攻技能仍可正常施放（比如焦点已设置为坦克,那么你的当前目标为友方时则对焦点的当前目标施法）

---

## 常见问题

### 按键不生效

1. 检查 GUI 中绑定按键是否正确（默认 XBUTTON1，鼠标侧键）
2. 确认 WoW 窗口在前台
3. 引导技能需使用 `hold` 模式

### 修改文件后不生效

1. 点击 GUI 中的"重载"按钮
2. 如果仍不生效，重启程序

---

## 调试命令

| 命令 | 说明 |
|------|------|
| `/fu gui` | 打开像素块调试界面 |
| `/fu message <文本>` | 向聊天框发送测试文本 |
| `/fu macro rebuild` | 手动重建所有动态宏 |
| `/script SetTestSecret(0)` | 关闭秘密值限制 |
| `/etrace` | 监控事件触发 |
